spring:
  application:
    # 服务名
    name: mall-payment
  cloud:
    nacos:
      # 服务端地址
      server-addr: localhost:8848
      # 服务注册
      discovery:
        # 命名空间
        namespace: 823e9089-86ec-43e4-a116-3a4365581c32
    sentinel:
      transport:
        # 服务地址
        dashboard: localhost:8080